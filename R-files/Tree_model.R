summary(final_data)
View(final_data)
install.packages('tree')
library(tree)
final_data$Exited<-as.factor(final_data$Exited)
View(final_data)
tree_based<-tree(final_data$Exited~.,data=final_data)
summary(tree_based)
plot(tree_based)
text(tree_based)
y_tree<-predict(tree_based,new_data=final_data,type='class')
confusionMatrix(final_data$Exited,y_tree)
